# 测试用例看板 - 产品需求文档 (PRD)

> **文档版本**: 1.0.0
> **创建日期**: 2026-01-20
> **作者**: AI Assistant
> **项目名称**: Test-Cases

---

## 1. 产品概述

### 1.1 产品背景

在软件开发过程中，测试用例的管理和追踪是质量保证的重要环节。目前团队已有标准化的测试用例文档（Markdown 格式），但缺乏可视化的管理工具来追踪测试执行状态。

### 1.2 产品目标

开发一个**本地化的测试用例看板系统**，具备以下核心能力：

1. **导入解析** - 支持导入 Markdown 格式的测试用例文档并自动解析
2. **分类管理** - 按页面/模块组织测试用例
3. **状态追踪** - 三种状态的看板式管理（待测试、有问题、已通过）
4. **本地存储** - 所有数据存储在本地，无需服务端

### 1.3 目标用户

- 前端开发工程师
- 测试工程师
- 产品经理

---

## 2. 功能需求

### 2.1 核心功能模块

```
┌─────────────────────────────────────────────────────────────────┐
│                      测试用例看板系统                              │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │   页面管理    │  │   文档导入    │  │      看板视图         │  │
│  │  - 页面列表   │  │  - MD 解析   │  │  - 三栏状态展示       │  │
│  │  - 页面切换   │  │  - 用例提取   │  │  - 分类折叠展开       │  │
│  │  - 页面新增   │  │  - 批量导入   │  │  - 卡片拖拽          │  │
│  └──────────────┘  └──────────────┘  └──────────────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │  用例详情     │  │  状态管理    │  │      数据持久化       │  │
│  │  - 完整信息   │  │  - 状态流转  │  │  - LocalStorage     │  │
│  │  - 测试步骤   │  │  - 批量操作  │  │  - 导入/导出         │  │
│  └──────────────┘  └──────────────┘  └──────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 页面设计

### 3.1 整体布局

```
┌────────────────────────────────────────────────────────────────────────────┐
│  🧪 测试用例看板                                    [导入文档] [导出数据]    │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  📑 页面选择: [ScriptPage ▼]    🔍 搜索用例...     📊 统计: 50/120   │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                                                            │
│  ┌────────────────────┬────────────────────┬────────────────────────────┐ │
│  │                    │                    │                            │ │
│  │    ⏳ 待测试        │    ❌ 有问题       │      ✅ 已通过              │ │
│  │    (35)            │    (5)             │      (80)                  │ │
│  │                    │                    │                            │ │
│  │  ┌──────────────┐  │  ┌──────────────┐  │  ┌────────────────────┐    │ │
│  │  │ 📁 台词列表   │  │  │ 📁 变量系统   │  │  │ 📁 台词列表         │    │ │
│  │  │    ▼ (8)     │  │  │    ▼ (3)     │  │  │    ▼ (15)          │    │ │
│  │  ├──────────────┤  │  ├──────────────┤  │  ├────────────────────┤    │ │
│  │  │ TC-SL-001    │  │  │ TC-VD-002    │  │  │ TC-SL-001          │    │ │
│  │  │ 正常加载列表  │  │  │ 检测多变量    │  │  │ 正常加载台词列表    │    │ │
│  │  │ P0 | 功能测试 │  │  │ P1 | 边界     │  │  │ P0 | 功能测试      │    │ │
│  │  └──────────────┘  │  └──────────────┘  │  └────────────────────┘    │ │
│  │  ┌──────────────┐  │  ┌──────────────┐  │  ┌────────────────────┐    │ │
│  │  │ TC-SL-002    │  │  │ TC-VF-001    │  │  │ TC-SL-002          │    │ │
│  │  │ 空列表状态    │  │  │ 打开填充面板  │  │  │ 空列表状态          │    │ │
│  │  │ P1 | 异常     │  │  │ P0 | 功能    │  │  │ P1 | 异常          │    │ │
│  │  └──────────────┘  │  └──────────────┘  │  └────────────────────┘    │ │
│  │                    │                    │                            │ │
│  │  ┌──────────────┐  │                    │                            │ │
│  │  │ 📁 搜索功能   │  │                    │                            │ │
│  │  │    ▶ (5)     │  │                    │                            │ │
│  │  └──────────────┘  │                    │                            │ │
│  │                    │                    │                            │ │
│  └────────────────────┴────────────────────┴────────────────────────────┘ │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 用例卡片详情展开

```
┌────────────────────────────────────────────────────┐
│  TC-SL-001: 正常加载台词列表                    [×] │
├────────────────────────────────────────────────────┤
│                                                    │
│  📌 基本信息                                        │
│  ├─ 编号: TC-SL-001                               │
│  ├─ 优先级: P0 (核心功能)                          │
│  ├─ 分类: 台词列表测试 > 列表加载                   │
│  └─ 当前状态: ⏳ 待测试                            │
│                                                    │
│  📋 前置条件                                        │
│  • 用户已登录，有台词数据                           │
│                                                    │
│  📝 测试步骤                                        │
│  1. 进入台词管理页面                                │
│  2. 等待页面加载完成                                │
│                                                    │
│  ✅ 预期结果                                        │
│  • 显示加载状态                                     │
│  • 列表正确显示所有台词                             │
│  • 每条台词显示名称、状态、档期日期、创建时间等      │
│                                                    │
│  📦 测试数据                                        │
│  • 至少 5 条台词记录                                │
│                                                    │
│  ─────────────────────────────────────────────────  │
│                                                    │
│  💬 测试备注                                        │
│  ┌──────────────────────────────────────────────┐  │
│  │ 输入测试备注...                               │  │
│  └──────────────────────────────────────────────┘  │
│                                                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐     │
│  │ ❌ 有问题 │  │ ✅ 通过  │  │ ⏳ 重置为待测 │     │
│  └──────────┘  └──────────┘  └──────────────┘     │
│                                                    │
└────────────────────────────────────────────────────┘
```

### 3.3 导入文档弹窗

```
┌────────────────────────────────────────────────────────────┐
│  📥 导入测试用例文档                                   [×]  │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  选择页面:                                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  ScriptPage (台词管理)                           ▼   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                            │
│  或新建页面:                                                │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  输入新页面名称...                                    │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                            │
│  上传文档:                                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                                                      │  │
│  │     📄 点击或拖拽 Markdown 文件到此处                  │  │
│  │                                                      │  │
│  │         支持 .md 格式                                 │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                            │
│  ⚠️ 导入说明:                                              │
│  • 将自动解析文档中的测试用例                               │
│  • 用例编号格式: TC-XX-000                                 │
│  • 重复编号将更新已有用例                                   │
│                                                            │
│  ┌────────────────────┐  ┌────────────────────────────┐   │
│  │       取消         │  │        确认导入            │   │
│  └────────────────────┘  └────────────────────────────┘   │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 4. 数据模型

### 4.1 页面 (Page)

```typescript
interface Page {
  id: string;           // 页面唯一标识
  name: string;         // 页面名称，如 "ScriptPage"
  displayName: string;  // 显示名称，如 "台词管理页面"
  description?: string; // 页面描述
  createdAt: number;    // 创建时间戳
  updatedAt: number;    // 更新时间戳
}
```

### 4.2 测试用例分类 (Category)

```typescript
interface Category {
  id: string;           // 分类唯一标识
  pageId: string;       // 所属页面 ID
  name: string;         // 分类名称，如 "台词列表测试"
  parentId?: string;    // 父分类 ID（支持层级）
  order: number;        // 排序顺序
}
```

### 4.3 测试用例 (TestCase)

```typescript
interface TestCase {
  id: string;              // 用例唯一标识
  code: string;            // 用例编号，如 "TC-SL-001"
  title: string;           // 用例标题
  pageId: string;          // 所属页面 ID
  categoryId: string;      // 所属分类 ID
  priority: 'P0' | 'P1' | 'P2';  // 优先级
  preconditions?: string;  // 前置条件
  steps: string[];         // 测试步骤
  expectedResults: string[]; // 预期结果
  testData?: string;       // 测试数据
  status: TestStatus;      // 测试状态
  notes?: string;          // 测试备注
  testedAt?: number;       // 测试时间
  createdAt: number;       // 创建时间
  updatedAt: number;       // 更新时间
}

type TestStatus = 'pending' | 'failed' | 'passed';
```

### 4.4 应用状态 (AppState)

```typescript
interface AppState {
  version: string;         // 数据版本
  pages: Page[];           // 页面列表
  categories: Category[];  // 分类列表
  testCases: TestCase[];   // 测试用例列表
  currentPageId?: string;  // 当前选中页面
  lastUpdated: number;     // 最后更新时间
}
```

---

## 5. 用户交互流程

### 5.1 首次使用流程

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   打开应用   │───▶│   空状态页   │───▶│   导入文档   │───▶│   看板展示   │
│             │    │   引导导入   │    │   选择页面   │    │   开始测试   │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
```

### 5.2 日常使用流程

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   选择页面   │───▶│  查看用例    │───▶│   执行测试   │───▶│   更新状态   │
│             │    │  (展开分类)  │    │   (查看详情) │    │  (通过/失败) │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
        │                                                       │
        │                                                       │
        └───────────────────────────────────────────────────────┘
                            循环测试
```

---

## 6. 功能详细说明

### 6.1 文档导入与解析

#### 6.1.1 支持的 Markdown 格式

系统将解析以下格式的测试用例：

```markdown
#### TC-XX-000: 用例标题
- **优先级**: P0/P1/P2
- **前置条件**: 条件描述
- **测试步骤**:
  1. 步骤一
  2. 步骤二
- **预期结果**:
  - 结果一
  - 结果二
- **测试数据**: 数据描述
```

#### 6.1.2 解析规则

1. **用例编号提取**: 匹配 `TC-[A-Z]+-\d+` 格式
2. **分类识别**: 根据 `## ` 和 `### ` 标题层级
3. **字段提取**: 识别加粗关键字后的内容
4. **优先级映射**: P0(核心) / P1(重要) / P2(次要)

### 6.2 看板管理

#### 6.2.1 三种状态

| 状态 | 图标 | 颜色 | 说明 |
|-----|------|------|------|
| 待测试 | ⏳ | 蓝色 | 未执行的用例 |
| 有问题 | ❌ | 红色 | 测试失败的用例 |
| 已通过 | ✅ | 绿色 | 测试通过的用例 |

#### 6.2.2 状态流转

```
                    ┌──────────────┐
                    │              │
        ┌───────────│    待测试    │───────────┐
        │           │   pending    │           │
        │           └──────────────┘           │
        │                  ▲                   │
        │                  │                   │
        ▼           重置为待测试                ▼
┌──────────────┐           │           ┌──────────────┐
│              │───────────┘           │              │
│    有问题    │◀─────────────────────▶│    已通过    │
│    failed    │       可相互切换       │    passed    │
└──────────────┘                       └──────────────┘
```

### 6.3 分类折叠功能

- 默认状态: 所有分类**展开**
- 点击分类头部可**折叠/展开**
- 折叠时显示分类名称和用例数量
- 支持**全部折叠/全部展开**快捷操作

### 6.4 数据持久化

- 使用 `localStorage` 存储所有数据
- 存储 Key: `test-cases-app-state`
- 支持数据**导出为 JSON 文件**
- 支持**导入 JSON 文件**恢复数据

---

## 7. 非功能性需求

### 7.1 性能要求

- 页面首次加载时间 < 1s
- 状态切换响应时间 < 100ms
- 支持单页面 500+ 测试用例

### 7.2 兼容性要求

- 支持 Chrome 90+
- 支持 Firefox 90+
- 支持 Safari 14+

### 7.3 数据安全

- 所有数据仅存储在本地
- 无网络请求
- 导出数据时提示备份

---

## 8. 里程碑计划

| 阶段 | 功能点 | 预计工期 |
|-----|--------|---------|
| Phase 1 | 基础框架 + 数据模型 + 存储 | 1天 |
| Phase 2 | MD 解析 + 文档导入 | 1天 |
| Phase 3 | 看板视图 + 状态管理 | 1天 |
| Phase 4 | 用例详情 + 交互优化 | 1天 |
| Phase 5 | 导入导出 + 测试完善 | 0.5天 |

---

## 9. 附录

### 9.1 术语表

| 术语 | 说明 |
|-----|------|
| 测试用例 (Test Case) | 描述一个具体测试场景的文档 |
| 看板 (Kanban) | 可视化管理工作流的方法 |
| 用例编号 | 如 TC-SL-001，TC=TestCase, SL=功能缩写, 001=序号 |

### 9.2 参考文档

- 测试用例文档示例: `docs/ScriptPage/测试用例文档.md`
- 测试用例格式规范: 见 6.1.1 节

